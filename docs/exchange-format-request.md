# Exchange-format request sent to `unused-assumptions`

Sent 3 September 2026, proxied by Carlo. Everything requested is additive. Check what arrived against this list before writing `tools/import_failures.py`; do not assume any item was implemented.

---

Request from the downstream project `unstated-conclusions` (the conclusion-side dual of this one; it consumes this project's exports and never writes to its stores). Everything below is **additive**: new fields and one new file, no changes to existing columns, ids, `verify.py` behaviour, or the paper's tables. If any item conflicts with your design, say so rather than bending it.

1. **Export the breaks, not only the survivors.** Add `data/breaks.jsonl`, generated by `tools/export.py` from the same stores, one row per (theorem, binder) where the descent stopped: for survivors, the first class *below* `holds_over` that failed; for non-survivors, the first class tried. Same schema as `survivors.jsonl` (`id`, `corpus`, `area`, `module`, `theorem`, `namespace`, `binder`, `stated`, `opens`, `source`, `signature`) plus: `fails_at` (the class that failed), `fail_verdict` (`needs_structure` / `incoherent` / `context` / timeout — keep the tool's own failures apart, as you already do), `fail_error` (the first error line, truncated to ~500 chars), and, when it can be parsed from the error, `blamed` (the constant name Lean names as needing the structure, e.g. the lemma or instance that failed to synthesise). The downstream use is dichotomy repair: `fails_at` says where the proof breaks, `blamed` says why.

2. **Add a `corpus` field to every row** (`Mathlib` | `TauCeti`) now that TauCeti is being swept, and make `MANIFEST.json` carry one entry per corpus — `{corpus, revision, describes, toolchain, project_path}` — instead of a single `mathlib` block. Keep the existing `mathlib` key too so nothing that reads it today breaks. For TauCeti rows, `module` is what a standalone file must import (root module is empty), so please confirm `module` is always the defining module, not a re-export.

3. **Document what `id` hashes.** Downstream needs to recompute it to join rows across exports and across the two projects. If it is a hash of (`corpus`, `theorem`, `binder`), say so in `MANIFEST.json` under `id_scheme` and keep it stable; if it currently includes anything that changes between runs (revision, class table), please add a second field `key` = `corpus + ":" + theorem + ":" + binder` that is deterministic by construction.

Optional, if cheap: `binder_index` (position of the weakened binder among the declaration's instance binders) so re-abstraction is unambiguous when two binders share a class, and `descent` (the ordered list of classes tried with a verdict each) so the break point can be recomputed from the row.

Nothing here needs a new sweep; it is a change to `export.py` and one extra query over the stores you already have.

---

## What `unstated-conclusions` does with each item

- `breaks.jsonl` → Track D input, replacing the read-only store snapshot described in BRIEF.md if it arrives; keep the snapshot path as fallback.
- `corpus` + per-corpus MANIFEST → the per-corpus pin check in the copied `verify.py`, and the rule that results are never pooled across corpora.
- `id_scheme` / `key` → the rung-2 join. Until it arrives, join on (`corpus`, `theorem`, `binder`) and record that in `MANIFEST.json`.
- `source` + `opens` (already present) → compile the weakened declaration in the sweep environment, read its proof term there, and `addDecl` the strengthened declaration from the subterm. No proof-term printing.
