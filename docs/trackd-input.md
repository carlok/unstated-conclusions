# Track D's input

Generated by `tools/trackd_report.py` from `data/trackd-input.json`. Every count here is read from the sibling's export; none is typed.

## Two populations, never merged

**Strict (rung 3): 10 rows.** rung 3: a binder of an unused-assumptions survivor that also broke, with a verdict that can host a dichotomy. Joined on `key = corpus:module:theorem:binder`.

**Top-up: 190 rows** drawn from a pool of 2548, seed 20260903. needs_structure rows that are their theorem's only break and are NOT survivors. Exercises the machinery. NOT rung 3, and no rate computed on it is a rung-3 rate.

## Why the strict population is ten and not two hundred

`BRIEF.md` budgets Part 1's pilot at roughly two hundred candidates drawn from the survivors' own rows in `breaks.jsonl.gz`. Joining the two files on `key` gives 277 rows: binders that both held somewhere and broke somewhere, which is the rung-3 shape.

267 of them broke with the verdict `incoherent`, which the sibling's own `docs/pipeline.md` defines as *the statement cannot be stated -- a sibling instance demands the stronger class*. Track D's target is `a'' -> b \/ c`, which needs an `a''` the statement can be written over. An incoherent break cannot carry one.

That is the sibling's definition of its own verdict, not a judgement made here. Ten rows remain.

## The ten

| theorem | area | stated | held at | broke at | exceptional case |
|---|---|---|---|---|---|
| `Ne.isUnit_C` | NumberTheory | `Field` | `DivisionRing` | `CommRing` | elements that are not units / zero divisors |
| `Polynomial.not_irreducible_C` | Algebra | `Field` | `DivisionRing` | `CommRing` | elements that are not units / zero divisors |
| `exists_rat_lt` | Algebra | `Field` | `DivisionRing` | `CommRing` | elements that are not units / zero divisors |
| `IsSepClosed.splits_codomain` | FieldTheory | `Field` | `CommRing` | `DivisionRing` | failure of commutativity |
| `Matrix.IsHermitian.hadamard` | LinearAlgebra | `CommMonoid` | `CommSemigroup` | `Monoid` | failure of commutativity |
| `IsLocalization.map_injective_of_injective'` | RingTheory | `IsDomain` | `NoZeroDivisors` | `Nontrivial` | the trivial ring (`Subsingleton`) |
| `LinearIndepOn.singleton` | LinearAlgebra | `IsDomain` | `NoZeroDivisors` | `Nontrivial` | the trivial ring (`Subsingleton`) |
| `LinearIndependent.of_subsingleton` | LinearAlgebra | `IsDomain` | `NoZeroDivisors` | `Nontrivial` | the trivial ring (`Subsingleton`) |
| `Module.End.HasUnifEigenvalue.isNilpotent_of_isNilpotent` | LinearAlgebra | `IsDomain` | `NoZeroDivisors` | `Nontrivial` | the trivial ring (`Subsingleton`) |
| `sbtw_iff_mem_image_Ioo_and_ne` | Analysis | `IsDomain` | `NoZeroDivisors` | `Nontrivial` | the trivial ring (`Subsingleton`) |

## What the shapes say before any prover runs

- **5**: the trivial ring (`Subsingleton`)
- **3**: elements that are not units / zero divisors
- **2**: failure of commutativity

`BRIEF.md` names the first of these in advance as the outcome that means the track produced a linter for degenerate-case clauses rather than theorems. It is the largest group here. The pilot is sized to detect that cheaply, not to avoid it.

One row deserves flagging before it is run: `LinearIndependent.of_subsingleton` is a theorem about subsingletons whose exceptional case is `Subsingleton`. Expect `restatement`.

## Fields that are not what they look like

- `blamed` is null on all 57,382 rows of the export and will stay so. The sweep kept only the first error line and Lean puts the class name on the second. The split property is derived from the class pair.

- `fails_at` is the sibling's *reconstruction*: its table proposes several classes per binder and tries each independently, so "the first class below" is not a recorded event. `descent` carries every class tried, and this project recomputes the break point under its own order.

- `fail_error` is capped at 200 characters by the store. There is no more to give without a four-day re-sweep.

- `context` rows are the sibling's tooling failing to reconstruct scope, not a fact about any theorem. Excluded from both populations as missing data.

