__pycache__/
*.pyc
.DS_Store

# Working data, not artefacts. What ships is under data/ as jsonl, generated by
# tools/export.py. The scratch traversal dumps are 57MB and regenerate in about
# forty minutes; keeping them would make the repository mostly intermediate
# state that nothing reads.
data/*.db
data/snapshots/
data/scratch/
runs/

paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk
paper/*.xdv

# The exchange folder with the sibling project: correspondence, not artefacts.
exchange/
